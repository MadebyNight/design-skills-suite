{
  "name": "skill-alipay-pages",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build:catalog": "node scripts/build-catalog.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
